# Guru Prasath Dharmaraj — HTMX Profile V2

Premium responsive portfolio built with HTML, CSS and HTMX.

Run locally:
`python -m http.server 8000`

Then open `http://localhost:8000`.

HTMX loads project/case-study partials without a full-page reload.
